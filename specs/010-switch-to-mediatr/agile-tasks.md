# Epic 010: Switch the backend's dispatch mechanism to MediatR

Source: specs/010-switch-to-mediatr/ (auto-generated by /agile-tasks - do not edit by hand; edit
tasks.md or spec.md and re-run instead)

Progress: 13/13 tasks (100%)

<!-- agile-tasks:groups
US1 -> Feature: Switch to MediatR
US2 -> Feature: Switch to MediatR
-->
<!-- agile-tasks:substories
US1 -> 1a: Handlers (T003,T004,T005,T006,T007,T008,T009,T010) | 1b: Endpoint wiring (T011)
US2 -> 2a: Foundational (T001,T002) | 2b: Polish (T012,T013)
-->

## Feature 001: Switch to MediatR

Progress: 13/13 tasks (100%)

### User Story 001 - Foundational (part of "Cross-cutting tasks")

Progress: 2/2 tasks (100%)

- [x] T001 Swap package references in `backend/src/SportBook.Api/SportBook.Api.csproj` and
      `backend/src/SportBook.Application/SportBook.Application.csproj`: remove
      `Mediator.Abstractions`/`Mediator.SourceGenerator` 3.0.2, add `MediatR` 14.2.0
      (research.md Decision) [P]
- [x] T002 Replace `builder.Services.AddMediator(options => options.ServiceLifetime =
      ServiceLifetime.Scoped)` with `builder.Services.AddMediatR(cfg =>
      cfg.RegisterServicesFromAssembly(typeof(SportBook.Application.ServiceCollectionExtensions)
      .Assembly))` in `backend/src/SportBook.Api/Program.cs` - MediatR's default Transient handler
      lifetime has no captive-dependency conflict with the scoped `SportBookDbContext`, so no
      lifetime override is needed (research.md gotcha)

### User Story 002 - Handlers (Priority: P1, part of "Use the ecosystem-standard dispatch library once the earlier objection is confirmed not to apply")

Progress: 8/8 tasks (100%)

- [x] T003 Auth slices (`backend/src/SportBook.Application/Features/Auth/`):
      `using Mediator;` -> `using MediatR;` in Login/Logout/Refresh/Register;
      `LogoutHandler.Handle` changed from `async ValueTask<Unit>` (ending `return Unit.Value;`) to
      `async Task` (no return statement) - MediatR's non-generic `IRequestHandler<TCommand>` has
      no `Unit` type [P]
- [x] T004 Availability slice
      (`backend/src/SportBook.Application/Features/Availability/GetAvailability/`):
      `using Mediator;` -> `using MediatR;`; `Handle` return type `async ValueTask<T>` ->
      `async Task<T>` [P]
- [x] T005 Bookings slices (`backend/src/SportBook.Application/Features/Bookings/`): same
      using-directive and return-type changes across CancelBooking/ConfirmBooking/
      CreateBooking/GetBookingById/ListMyBookings/ListVenueBookingsForOwner - CreateBooking's
      serializable-transaction deadlock-retry loop untouched [P]
- [x] T006 Cities slices (`backend/src/SportBook.Application/Features/Cities/`): same
      changes across FindNearestCity/SuggestCities [P]
- [x] T007 Courts slices (`backend/src/SportBook.Application/Features/Courts/`): same
      changes across CreateCourt/ListCourtsByVenue/UpdateCourt; `DeleteCourtHandler.Handle`
      changed from `async ValueTask<Unit>` to `async Task`, same as T003's Logout case [P]
- [x] T008 Reviews slices (`backend/src/SportBook.Application/Features/Reviews/`): same
      changes across CreateOrReplaceReview/ListReviewsByVenue [P]
- [x] T009 Users slice
      (`backend/src/SportBook.Application/Features/Users/GetMe/`): same changes [P]
- [x] T010 Venues slices (`backend/src/SportBook.Application/Features/Venues/`): same
      changes across CreateVenue/SearchNearbyVenues/SearchVenues/UpdateVenue;
      `GetVenueByIdHandler.Handle` simplified from `new(reader.GetByIdAsync(...))` (constructing a
      `ValueTask<T>` from a `Task<T>`) to returning `reader.GetByIdAsync(...)` directly, since
      `Task<T>` needs no such wrapping; `DeleteVenueHandler.Handle` changed from
      `async ValueTask<Unit>` to `async Task`, same as T003/T007's pattern [P]

### User Story 003 - Endpoint wiring (Priority: P1, part of "Use the ecosystem-standard dispatch library once the earlier objection is confirmed not to apply")

Progress: 1/1 tasks (100%)

- [x] T011 Wire all 8 `backend/src/SportBook.Api/Endpoints/*.cs` files:
      `using Mediator;` -> `using MediatR;` - no other change, since `IMediator`/`mediator.Send`
      have the identical shape in both libraries

### User Story 004 - Polish (part of "Cross-cutting tasks")

Progress: 2/2 tasks (100%)

- [x] T012 Drop the now-invalid `.AsTask()` call (used to bridge martinothamar's
      `ValueTask<T>` to xUnit's `Task`-based `Assert.ThrowsAsync`) from
      `backend/tests/SportBook.UnitTests/BookingOverlapCheckTests.cs`,
      `ReviewEditCommentTests.cs`, `ReviewEditWindowTests.cs`, and `ReviewEligibilityTests.cs` -
      `Task<T>` already satisfies the expected signature (research.md gotcha) [P]
- [x] T013 Run the full quickstart.md verification: clean `dotnet build` (0 errors, only the
      pre-existing `NU1510`/`NU1903` warnings), 51 unit + 72 integration tests green, manual curl
      checklist (login, logout - the trickiest case, a void-command handler) against a live server
