# Epic 003: Geolocation-centered radius map of nearby venues

Source: specs/003-venue-radius-map/ (auto-generated by /agile-tasks - do not edit by hand;
edit tasks.md or spec.md and re-run instead)

Progress: 25/25 tasks (100%)

<!-- agile-tasks:groups
US1 -> Feature: Nearby venues radius view
US2 -> Feature: Nearby venues radius view
US3 -> Feature: Nearby venues radius view
-->
<!-- agile-tasks:substories
US4 -> 4a: Backend nearby endpoint (T002,T003,T004) | 4b: Frontend foundational (T001,T005,T006,T007,T008) | 4c: Polish & verification (T019,T020,T021,T022,T023,T024,T025)
-->

## Feature 001: Nearby venues radius view

Progress: 25/25 tasks (100%)

### User Story 001 - Backend nearby endpoint (part of "Cross-cutting tasks")

Progress: 3/3 tasks (100%)

- [x] T002 Add `NearbyVenueResponse` DTO (venue summary + `decimal DistanceKm`) in
      `backend/src/SportBook.Application/Dtos/VenueDtos.cs` per data-model.md [P]
- [x] T003 Implement `VenueService.SearchNearbyAsync` (a `VenueRadiusKm = 75` constant beside
      `VenueService`; query `Venues` where `Latitude != null` (+ active-court sport predicate when
      `sportType` set), `Include(v => v.City)`; then C# `CityDistance.DistanceKm`, filter <= 75,
      order ascending, `Take(100)`, project) in
      `backend/src/SportBook.Application/Services/VenueService.cs` (depends on T002)
- [x] T004 Implement `VenuesController` `GET /api/venues/nearby` (`lat`, `lng`, optional
      `sportType`; range-validate; never persist or log received coordinates) per contracts/api.md
      in `backend/src/SportBook.Api/Controllers/VenuesController.cs` (depends on T003)

### User Story 002 - Frontend foundational (part of "Cross-cutting tasks")

Progress: 5/5 tasks (100%)

- [x] T001 Add frontend dependencies `react-leaflet-cluster` (~4.1.3) and dev
      `@types/leaflet.markercluster` (~1.5.6) to `frontend/package.json` (user-approved via the
      clustering choice in /speckit-clarify); confirm `leaflet.markercluster` is pulled
      transitively [P]
- [x] T005 Extract a `useGeolocation` hook (browser Geolocation API, 2-decimal rounding,
      permission/denied/error state) from the inline logic in `MyCityButton` into
      `frontend/src/shared/lib/` [P]
- [x] T006 Implement a `useReferencePoint` resolver (granted device location via `useGeolocation`
      -> explicitly selected city coordinates -> none) as the single source of truth for the map
      center and the nearby query, in `frontend/src/shared/lib/` (depends on T005) [P]
- [x] T007 Frontend: `NearbyVenue` type (+`distanceKm`) and `getNearbyVenues(lat, lng, sportType?)`
      API call in `frontend/src/entities/venue/` (depends on T004) [P]
- [x] T008 Extend `frontend/src/shared/ui/map/MapView.tsx`: marker clustering via
      `react-leaflet-cluster` (still lazy), a `fitBounds` mode (a `useMap` effect keyed on the
      reference/venue-id set, fit once per reference change with a max-zoom cap, never re-fitting
      on unrelated renders), and a per-marker emphasis icon (a second `L.icon` for the nearest
      venue - never `L.divIcon({ html })` with venue fields) (depends on T001)

### User Story 003 - See venues near me on a map (Priority: P1)

Progress: 6/6 tasks (100%)

A customer opens the venue search experience and activates the "near me" action to share their
location; they then see a map centered on where they are, showing the venues within 75 km that
they could travel to, clustered and auto-framed with the nearest emphasized, and a results list
below showing the same venues nearest-first.

- [x] T009 Integration test: `GET /api/venues/nearby` returns venues within 75 km ordered
      nearest-first with `distanceKm`, excludes venues beyond 75 km, rejects out-of-range
      `lat`/`lng` (400), honors `sportType`, and requires auth in
      `backend/tests/SportBook.IntegrationTests/VenueNearbyPointTests.cs` [P]
- [x] T010 Unit test: the nearby distance/order/cap over materialized rows (Sqlite path) in
      `backend/tests/SportBook.UnitTests/VenueNearbyDistanceTests.cs` [P]
- [x] T011 Unit test: `ToQueryString()` proves the `Latitude != null` (+ optional sport) candidate
      query translates to SQL and pushes NO trigonometry server-side in
      `backend/tests/SportBook.UnitTests/VenueNearbyQueryTranslationTests.cs` [P]
- [x] T012 Frontend: "near me" action in `frontend/src/features/city-select/` using `useGeolocation`
      to set the device-location reference point (rounds coords to 2 decimals before any request)
      (depends on T005, T006)
- [x] T013 Frontend: reshape `frontend/src/pages/venues/ui/VenueSearchPage.tsx` to the
      reference-point radius view - when a reference is active, call `getNearbyVenues` and render
      the clustered `MapView` (nearest emphasized, auto-framed) plus a distance-ordered results
      list from the same in-range set; remove the `VenueSearchMap` usage and the `includeNearby`
      toggle (depends on T006, T007, T008, T012)
- [x] T014 Frontend smoke test (map + geolocation mocked): the near-me flow shows the in-range
      venues on the map and in the list nearest-first in
      `frontend/tests/pages/VenueRadiusView.test.tsx` [P]

### User Story 004 - See venues near a chosen city (Priority: P2)

Progress: 2/2 tasks (100%)

A customer who does not share their device location instead picks a city from the directory, and
the same nearby-venues map appears centered on that city, showing venues within 75 km of it, with
the results list showing the same in-range venues.

- [x] T015 Frontend: wire city selection into `useReferencePoint` (a selected city's coordinates
      become the reference when no device location is active) so the combobox drives the same
      radius map + list in `frontend/src/pages/venues/ui/VenueSearchPage.tsx` (depends on T013)
- [x] T016 Frontend test: selecting a city with no geolocation drives the radius map and the
      distance-ordered list; device location takes precedence over a selected city when both exist,
      in `frontend/tests/pages/VenueRadiusView.test.tsx`

### User Story 005 - No misleading map when there is nothing to center on (Priority: P3)

Progress: 2/2 tasks (100%)

A customer who has neither granted location nor selected a city sees no map at all - not an empty
map, not a placeholder - so they are never shown an empty or "no venues found" map next to results
that actually exist elsewhere.

- [x] T017 Frontend: the no-reference state in `frontend/src/pages/venues/ui/VenueSearchPage.tsx` -
      render no map block at all (not an empty map), and show a results-list prompt to pick a city
      or use "near me"; removing the reference removes the map block rather than showing it empty
      (depends on T013)
- [x] T018 Frontend test: no geolocation and no selected city renders no map block and shows the
      prompt in `frontend/tests/pages/VenueRadiusView.test.tsx`

### User Story 006 - Polish & verification (part of "Cross-cutting tasks")

Progress: 7/7 tasks (100%)

- [x] T019 Add near-me / no-reference-prompt / cluster-related i18n keys in
      `frontend/src/shared/i18n/locales/{en,uk,pt}.json` (all three locales) [P]
- [x] T020 Remove the superseded "My city" button (its geolocation role is replaced by "near me")
      and any now-dead `includeNearby` wiring left on the search page; confirm no dead code or
      unused imports in `frontend/src/features/city-select/` and `frontend/src/pages/venues/`
- [x] T021 Measure `yarn build` output before/after the clustering libs land from `frontend/` -
      confirm the initial JS chunk delta is 0 (leaflet/react-leaflet/react-leaflet-cluster/
      leaflet.markercluster all in the lazy `MapView` chunk; spec SC-006, quickstart.md)
- [x] T022 Response-DTO whitelist audit for `NearbyVenueResponse` - confirm `Population` never leaks
      and no new `[AllowAnonymous]` was introduced (contract MUST, spec FR-011) [P]
- [x] T023 Update `backend/README.md`/`frontend/README.md`/root `README.md` with the nearby
      endpoint, the clustering dependency, and the "near me" action [P]
- [x] T024 Verify the EF Core query plan for `GET /api/venues/nearby` - confirm the only
      server-side work is the `Latitude != null` (+ sport) filter and that no trigonometric
      full-table scan is generated (performance consilium finding)
- [x] T025 Run all quickstart.md validation scenarios end-to-end against a locally running stack [P]
